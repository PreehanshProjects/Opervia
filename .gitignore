node_modules/
dist/
.env
.env.*
!.env.example
*.local
test-results/
playwright-report/
*.tsbuildinfo
tmp/

# Capacitor: generated native build output. The android/ project itself is
# committed (it holds our PrintPlugin and manifest), but its build artefacts
# and the copied web bundle are not.
android/app/src/main/assets/public/
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/release/
android/capacitor-cordova-android-plugins/
*.keystore
!android/app/src/main/assets/capacitor.config.json
